{
  "name": "rushi",
  "private": true,
  "description": "如是我闻 — 本地中文转写与校对（独立仓）",
  "scripts": {
    "check:doc-links": "node scripts/check-internal-doc-links.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
