{
  "name": "@rushi/wasm-waveform",
  "version": "0.1.0",
  "description": "Canvas 2D waveform renderer via wasm-bindgen for Rushi",
  "type": "module",
  "main": "pkg/wasm_waveform.js",
  "types": "pkg/wasm_waveform.d.ts",
  "files": ["pkg"],
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg",
    "build:dev": "wasm-pack build --target web --out-dir pkg --dev",
    "clean": "rm -rf pkg target"
  },
  "license": "ISC"
}
