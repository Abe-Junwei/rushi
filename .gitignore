.DS_Store
*.log
.env
.env.*
!.env.example
node_modules/
dist/
# Offline ASR model pack build cache (~1.2 GB; Route E — deprecated)
# Bundled ASR models staged for Tauri release (~1.2 GB; Plan B)
dist/bundled-asr-models/
apps/desktop/src-tauri/resources/bundled-asr-models/modelscope/
coverage/
apps/desktop/coverage/
target/
# Tauri 打包产物（stage-release-artifacts.sh 复制 DMG + symlink .app 到仓库根；勿提交）
/*.dmg
/*.app
/如是我闻.app
# Windows 本地发版 zip — 默认写到 E:\rushi-artifacts\win-release（见 Get-RushiWinArtifactDir）；勿落仓库根
/如是我闻_*.zip
/windows-portable-x64.zip
/rushi-asr-sidecar-cuda*.zip
.venv/
.venv-ci-tools/
__pycache__/
*.egg-info/
.python-version

# P0 本地生成的中文试听样本（不提交二进制）
fixtures/p0-samples/*.wav
fixtures/p0-samples/*.aiff
fixtures/p0-samples/*.caf

# P4 评测占位 wav（本地 `npm run p4:eval-placeholders` 生成，不提交）
fixtures/p4-eval/samples/*.wav

# R3h-3.5 Sherpa spike：本地下载的 ONNX 权重（~200MB+，勿提交）
fixtures/sherpa-paraformer-*/
fixtures/sherpa-qwen3-*/
fixtures/sherpa-vad/
fixtures/sherpa-punctuation-*/
# Spike 评估 JSON 输出（手测生成，勿提交）
docs/execution/spike-output/

# Agent / 手测临时文件（勿提交）
docs/tmp/

# 本机 LRC / runtime manifest 历史 fixture（v0.1.8 前脚本生成，含侧车 zip，勿提交）
fixtures/local-runtime/**/*.zip
fixtures/local-runtime/**/rushi-runtime-manifest*.json

# PyInstaller 侧车 ASR（由 scripts/build-asr-sidecar-* 生成，体积大，不提交）
apps/desktop/src-tauri/resources/bundled-asr/rushi-asr-sidecar/
apps/desktop/src-tauri/resources/bundled-asr/rushi-asr-sidecar-cuda/
# Tauri build: autogenerated per-command ACL (from build.rs AppManifest)
apps/desktop/src-tauri/permissions/autogenerated/
services/asr/.venv-sidecar-build/
services/asr/.venv-sidecar-build-cuda/
# Locked-venv rename leftovers (build script now prefers E:\rushi-artifacts\sidecar-venv-stale)
services/asr/.venv-sidecar-build*.stale-*/
services/asr/.venv-sidecar-build*.stale-*
# 本地生成 lockfile / 依赖矩阵用 venv（含 torch 等，文件极多；勿提交、勿让 IDE 全量索引）
services/asr/.venv-lockgen-cpu/
services/asr/.venv-audit/
services/asr/rushi-asr-sidecar.spec
services/asr/rushi-asr-sidecar-cuda.spec
services/asr/build/
services/asr/dist/
services/asr/third_party/ffmpeg/darwin-arm64/
services/asr/third_party/ffmpeg/darwin-x64/
services/asr/third_party/ffmpeg/win32-x64/
services/asr/third_party/ffmpeg/linux-x64/
.playwright-mcp/

# 本机安装笔记（勿提交）
/mac版安装方式.rtf

# Local machine move helper / leftover pack checksums (do not commit)
finish-move-from-c.bat
*.sha256
