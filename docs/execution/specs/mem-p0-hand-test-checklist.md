# MEM-P0 — 纠错记忆（简化规则）手测清单

> **状态**：✅ 手测签收（2026-06-04）

> **产品规则（2026-06）**  
> 1. 选中错词 → 右键 **纳入更正记忆** → 输入正词（例 `智控`→`制控`）= **开始学习**（记 1 次）。  
> 2. 之后在语段里把同一错词改成正词，**每次保存都计次**（**自动保存**与 **手动保存** 相同）。  
> 3. 同一错→对累计 **≥3 次** → **稳定记忆**（可用「纠错规则」全文替换）+ **自动加入术语表**（改善下次转写）。

## 机器闸门

```bash
bash scripts/r3-5f-b-machine-gate.sh
```

## §1 — 纳入记忆起步

1. 选中 `智控` → 右键纳入 → 正形 `制控` → 确认。  
2. 打开 **纠错记忆**：应有 `智控→制控`，次数 **1**。  
3. 术语表 **尚无** `制控`（未满 3 次）。

## §2 — 改词计次（自动 + 手动保存）

1. 在语段把 `智控` 改为 `制控`，停笔等 **自动保存**（约 2s）。  
2. 纠错记忆次数应 **+1**（变为 2）。  
3. 再改另一处 `智控`→`制控`（或同段再保存一次），点 **手动保存**。  
4. 次数应 **≥3**；**术语表** 出现 `制控`（可带别名 `智控`）；**无需** 再点进表弹窗。

## §3 — 稳定后 F1

1. 次数 ≥3 后，点 **纠错规则** → 预览应含 `智控→制控` → 确认写回。  
2. 全文相应替换生效。

---

## 签收

| 日期 | 结果 | 备注 |
|------|------|------|
| 2026-06-04 | ✅ | 纳入记忆 → 保存计次 → ≥3 自动术语表 + F1 写回 |
