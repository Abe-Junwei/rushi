# Release packaging audit — 2026-06

> 12-batch zero-dead-angle audit for macOS personal v1 release.  
> **主控策略**：[`release-parity-program-2026-06.md`](./release-parity-program-2026-06.md)；本文保留为 2026-06 packaging evidence，不再作为 release parity 主真源。  
> Evidence baseline for waveform / sidecar / bundle path fixes in this cycle.

## Summary

| Area | Status | Notes |
|------|--------|-------|
| Build gates | ✅ | `desktop:build-app` runs sidecar preflight; `release:mac` full gate |
| Bundle paths | ✅ | Tauri nests `resources/*` → `Contents/Resources/resources/bundled-asr/` |
| ASR sidecar | ✅ | Preflight + smoke; CI builds sidecar on macOS/Linux/Windows |
| Waveform / asset | ✅ | CSP + asset scope + startup registration; probe script |
| App Data | ✅ | `resolve-app-data-root.sh`; legacy nested path in scope |
| Release copy | ✅ | `isPackagedDesktopApp()` / `dev_or_packaged_str` sweep |
| CI release.yml | ✅ | macOS/Windows sidecar build before Tauri |
| Hand-test | 📋 | See [release-zero-terminal-hand-test.md](../release-zero-terminal-hand-test.md) |

## Batch 0 — Evidence baseline

- **Symptom**: Release `.app` ~1GB; ASR/transcribe OK; waveform stuck on「正在加载波形…」; smoke script false FAIL on ffmpeg path.
- **Logs**: `desktop.log` — look for `asset_scope_ok`, `ui waveform`, `bundled_sidecar_*`.
- **Peaks on disk**: `~/Library/Application Support/studio.lingchuang.rushi/studio.lingchuang.rushi/projects/<id>/peaks/*.dat`
- **Bundled sidecar**: `.app/Contents/Resources/resources/bundled-asr/rushi-asr-sidecar/`
- **DevTools in release**: `RUSHI_DEVTOOLS=1 open …app` (after rebuild with `devtools` feature)

## Batch 1 — Build gates

| Check | Command | Expected |
|-------|---------|----------|
| Typecheck + test + guard | `npm run typecheck && npm run test && node scripts/check-architecture-guard.mjs` | pass |
| Sidecar preflight | `npm run release:sidecar-preflight` | bundled exe + ffmpeg present |
| App build | `npm run desktop:build-app` | preflight → `.app` |
| Full mac release | `npm run release:mac` | above + r9-rel-1 + evidence md |
| Post-build verify | `npm run release:postbuild-verify` | ffmpeg/ffprobe/sidecar + waveform probe |

## Batch 2 — Bundle paths

- **Correct ffmpeg path in .app**: `Contents/Resources/resources/bundled-asr/rushi-asr-sidecar/_internal/ffmpeg`
- **Script**: `scripts/resolve-bundled-tool-in-app.sh`
- **Smoke**: `scripts/v1-release-installed-smoke.sh` uses resolve scripts (not stale `bundled-asr/` at Resources root)
- **Size note**: DMG compressed ≠ `.app` on disk (~1GB with sidecar + ffmpeg)

## Batch 3 — ASR sidecar

- Build: `npm run asr:build-sidecar-unix`
- Smoke: `npm run asr:smoke-sidecar`
- Installed smoke: `bash scripts/v1-release-installed-smoke.sh`
- Models remain in App Data `models/`, not in install bundle

## Batch 4 — Waveform / asset scope

Fixes landed:

1. `tauri.conf.json` — CSP `asset:` + `$APPDATA/**/*` scope
2. `project/asset_scope.rs` — startup + per-import registration
3. Frontend — `desktopUiLog`, peaks error banner, render status copy
4. WaveSurfer — production `style-src` includes `'unsafe-inline'` (shadow DOM `<style>`); optional `cspNonce` probe + `applyWaveSurferShadowCspNonce` for Q-CSP-1 hardening path
5. Probe: `npm run release:waveform-probe`

Verify after rebuild:

```bash
grep -E 'asset_scope|ui waveform' \
  ~/Library/Application\ Support/studio.lingchuang.rushi/studio.lingchuang.rushi/logs/desktop.log | tail -20
```

## Batch 5 — App Data

- `scripts/resolve-app-data-root.sh` — canonical legacy nested root
- Fresh install: open app → import project → peaks under resolved root

## Batch 6 — Release copy

- TS: `apps/desktop/src/services/packagedUserHints.ts` + `isPackagedDesktopApp()`
- Rust: `apps/desktop/src-tauri/src/packaged_hints.rs` + `cfg!(debug_assertions)`
- Intentional dev-only strings remain in comments / dev-only code paths (`source.rs` restart, README)

## Batch 7 — Transcribe / export / LLM

- `local_transcribe_gate.rs` — packaged async-transcribe + ffmpeg hints
- `transcribePreviewState.ts` — `transcribeAsyncFallbackHint()`
- Quality eval batch run remains dev-only (requires repo `scripts/eval-run.py`)

## Batch 8 — Platform (DMG / sign)

- DMG: optional via `desktop:build-dmg` or `RUSHI_RELEASE_BUNDLE=dmg`; cleanup script avoids stale `rw.*.dmg`
- Codesign / notarization: **not in scope** for personal v1; document for store track

## Batch 9 — CI

`.github/workflows/release.yml`:

- **Linux**: sidecar build + smoke (unchanged)
- **macOS**: + Python, ffmpeg, sidecar build + smoke before Tauri
- **Windows**: + Python, sidecar build + artifact verify before Tauri

## Batch 10 — Security

- Local token mismatch → user copy points to「重试内置侧车」in release
- CSP tightened for asset protocol; no broad filesystem read beyond scoped media

## Batch 11 — Doc alignment

- Build evidence: `docs/execution/v1-release-build-evidence.md` (generated by `release:mac`)
- This audit supersedes stale path references in older evidence files

## Batch 12 — Zero-terminal signoff

See [release-zero-terminal-hand-test.md](../release-zero-terminal-hand-test.md) and [dmg-vs-dev-parity-checklist.md](./dmg-vs-dev-parity-checklist.md) (WKWebView 专项).

## Rebuild checklist (operator)

```bash
npm run asr:build-sidecar-unix
npm run release:sidecar-preflight
npm run desktop:build-app
npm run release:postbuild-verify
bash scripts/v1-release-installed-smoke.sh
open apps/desktop/src-tauri/target/release/bundle/macos/如是我闻.app
# 或根目录：open 如是我闻.app / 安装 open 如是我闻_0.1.0_aarch64.dmg
```

Optional debug: `RUSHI_DEVTOOLS=1 open …app`
